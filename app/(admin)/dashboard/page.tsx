
import React from "react";
import Typography from "@mui/material/Typography";

export const dynamic = "force-dynamic"; // Ensures homepage is dynamically rendered

export default function HomePage() {
  return (
    <Typography>
      Welcome to Crowd Level
    </Typography>
  );
}
